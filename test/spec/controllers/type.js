//'use strict';

//describe('<type>', function() {

  //var page  = '/mocks/type.html';

  //beforeEach(function() {
    //browser().navigateTo(page);
  //});

  //describe('with header', function() {

    //it('sets the type name', function() {
      //expect(element('.type .header .name').text()).toBe('Basic Light');
    //});

    //it('sets the type description', function() {
      //expect(element('.type .header .description').text()).toMatch('The Basic Light type represents the most basic light structure.');
    //});

    //describe('with options', function() {

      //describe('when default', function() {

        //it('activates properties ', function() {
          //expect(element('.type .header .options .active').text()).toBe('5 Properties');
        //});

        //it('shows properties', function() {
          //expect(element('.type .sections .properties').css('display')).toBe('block');
        //});
      //});

      //describe('when clicks functions', function() {

        //beforeEach(function() { element('.type .header .show-functions').click(); });

        //it('activates properties ', function() {
          //expect(element('.type .header .options .active').text()).toBe('5 Functions');
        //});

        //it('shows functions', function() {
          //expect(element('.type .sections .functions').css('display')).toBe('block');
        //});
      //});

      //describe('when clicks statuses', function() {

        //beforeEach(function() { element('.type .header .show-statuses').click(); });

        //it('activates properties ', function() {
          //expect(element('.type .header .options .active').text()).toBe('2 Statuses');
        //});

        //it('shows functions', function() {
          //expect(element('.type .sections .statuses').css('display')).toBe('block');
        //});
      //});
    //});
  //});
//});
